public OnDialogResponse(playerid, dialogid, response, listitem, inputtext[])
{

	new Float:posX, Float:posY, Float:posZ;
	GetPlayerPos(playerid, posX, posY, posZ);
	if(dialogid == VEHRENTDIALOG)
    {
		if(response)
		    {
		     	switch(listitem)
			    {
	
				}	
		if(!response)
		{
		   
		}

	}
	return 1;
}